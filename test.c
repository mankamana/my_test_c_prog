#include <stdio.h>
#include <string.h>
#include <stdlib.h>
    int main()
    {
        struct p
        {
            char *name;
            struct p *next;
        } temp;
struct p *ptrary[10];
        struct p *p_;
        p_ = malloc (sizeof(struct p));
            free(p_);
        while (1) {
        p_ = malloc (sizeof(struct p));
        }
        return 0;
    }
